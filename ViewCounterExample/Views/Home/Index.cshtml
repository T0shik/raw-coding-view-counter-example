@model IEnumerable<ViewCounterExample.Models.Post>

@{ 

}

    <div>
        @foreach(var post in Model)
        {
            <h1>@post.Title</h1>
            <p>Views: <strong>@post.ViewsCount</strong></p>
        }
    </div>